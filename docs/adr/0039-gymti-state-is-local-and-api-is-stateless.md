# GYMTI 状态本地持久化且 API 无会话

状态：已接受（2026-07-23）

GYMTI 的一个进行中尝试、一个待展示结果、一个当前结果及已确认教练风格都进入主应用现有 Dexie 数据库，Dexie 是流程恢复与结果生命周期的唯一持久化权威。API 不维护进程内会话或第二套问卷数据库；每次根据最小稳定 ID 与版本重新校验和计分，再调用 LLM，并且不保存请求正文、逐题回答、训练档案或结果。我们接受服务端每次复算的少量成本，以换取刷新可恢复、模型故障可本地降级、无服务端会话漂移，以及与 TrainPal 现有本地优先数据架构一致的边界。
