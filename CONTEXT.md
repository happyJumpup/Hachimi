# TrainPal 训练体验

定义用户如何把自己找到并有权使用的健身视频，通过本地上传转化为可执行训练，并在 TrainPal 陪伴下完成。

## Language

**独立训练产品**:
承载动作理解、训练编排、训练执行和结果回顾完整体验的产品本体。它可以由抖音内容触发，但不以嵌入抖音信息流为成立条件。
_Avoid_: 抖音内嵌功能、信息流插件

**内容入口**:
用户把想练的来源视频带入独立训练产品的触发位置。下一轮原型以本地视频导入为主，以受控视频源兜底；抖音是首要内容发现渠道，但内容入口本身不承载后续训练闭环。
_Avoid_: 产品本体、训练页面

**本地视频导入**:
用户主动从当前设备选择自己有权使用的视频文件，并将其带入动作理解与训练编排流程。它不读取或复用平台 Cookie，也不通过任意分享链接抓取第三方媒体。
_Avoid_: 平台登录态导入、Cookie 下载、任意链接解析

**本地来源媒体**:
用户导入后保存在当前设备、供方案编辑、刷新恢复、稍后训练和复练继续播放的原视频。服务端仅接收分析所需的临时副本并在运行结束后清除；首版不建设云端视频库，也不承诺跨设备可用。若浏览器无法继续访问本地媒体，产品必须明确要求用户重新选择文件，不得静默替换来源。
_Avoid_: 云端视频资产、跨设备媒体、永久服务端上传、一次性预览文件

**TrainPal**:
独立训练产品、用户可见的唯一 Agent 和小猫教练形象共同使用的名称。三者是同一个训练伙伴在产品、决策和呈现层的不同角色，不拆成多个用户身份。
_Avoid_: 拼写错误、旧品牌名、独立 Pet 名、抖音内嵌功能名

**刷到就是练到**:
TrainPal 表达“把用户已经找到并想练的健身视频转成可执行训练”的品牌主张。当前独立 Web 的真实路径是用户先保存或拥有视频，再本地选择并交给 TrainPal；它不是已实现的抖音信息流一键入口。未来若平台开放内容理解或分享能力，可以缩短入口，但比赛演示不得伪装已经接入。
_Avoid_: 当前入口事实、抖音内嵌承诺、虚构平台接口、无需上传

**平台内容理解迁移愿景**:
训练编译、个性化调整和动作要点补充能力可以在未来承接抖音 AI 等平台已经生成的结构化内容理解结果，减少重复分析；当前产品仍使用本地上传和 TrainPal 自建 Provider，不调用、模拟调用或暗示已获得未开放的平台接口。
_Avoid_: 当前实现说明、AI 抖音调用、平台合作承诺、伪造接口

**旅程页面**:
围绕用户旅程中的一个主要任务设计的独立页面。首版依次使用首页/导入、分析、方案、首次个性化步骤流、训练中、训练结果和“我的”；分析完成后进入方案，不在同一页面同时堆叠上传、分析、候选、编排和训练。训练页隐藏全局导航，其他页面只保留与当前任务直接相关的主要操作。
_Avoid_: 单页功能墙、抖音视频页侧栏、全流程巨型页面、训练中全局导航

**首页导入**:
TrainPal 的独立产品首页，只承担解释核心价值、选择本地健身视频和找回少量最近项目。选片后显示缩略图、文件名、时长、本地处理说明和“开始分析”，不模拟信息流、不自动分析，也不在首页展开分析结果或方案编辑器。
_Avoid_: 抖音信息流、自动上传分析、首页方案编辑、功能总览墙

**分析任务页**:
用户显式开始分析后进入的专注页面，只展示来源预览、真实处理阶段、覆盖进度、已发现动作数和取消或重试。用户可以离开且任务继续；完成后显示“训练方案已准备好”并由用户主动进入方案，不强制跳转或在本页展开编辑器。
_Avoid_: 无限加载页、伪百分比、自动跳转、候选与方案同屏、失败即空结果

**TrainPal 主导航**:
独立产品的三个顶层入口：`首页` 用于导入与最近项目，`训练` 用于当前方案、继续未完成训练和已保存方案，`我的` 用于训练记录、GYMTI、个人信息、教练风格、成长与设置。分析、方案编辑、GYMTI 步骤流、训练和结果使用顶部返回的沉浸式子流程，不显示底部导航。
_Avoid_: 待练/方案/记录三分法、五项底部导航、子流程双重导航、训练页底栏

**全局继续训练入口**:
存在未完成训练时，所有非训练页面显示的统一轻量入口，点击后恢复同一训练场次。它不复制完整训练控制、不自动开始下一组，也不与页面当前主操作竞争。
_Avoid_: 多处不同恢复入口、后台自动训练、全局控制条、遮挡主按钮

**移动优先响应式**:
先为单手触控、窄屏、安全区和软键盘设计完整主流程，再为更宽屏幕渐进增加辅助栏或并排预览。移动端使用单列、至少 44px 触控目标、底部固定主操作和按需 Bottom Sheet；桌面端不渲染固定 390px 手机壳，也不改变页面的唯一主任务。
_Avoid_: 桌面手机模拟器、桌面缩放版、移动端右侧栏、仅桌面可完成

**TrainPal 视觉语言**:
以“温暖训练手账 × 小猫教练”为独立品牌方向：规划、个性化和结果页面使用温暖明亮底色、深色正文、大号训练数字与明显留白；训练页使用高对比深色舞台保证视频、计时和控制可读。小猫教练通过教练卡、贴纸式状态和阶段反馈出现，整体有运动感与陪伴感但不儿童化。
_Avoid_: 全站黑底、电青珊瑚霓虹、抖音信息流框架、儿童宠物应用、通用 AI 渐变

**训练舞台**:
训练中页面用于集中呈现当前动作、来源视频、训练进度、TrainPal 和必要控制的高对比界面。它可以使用深色视觉，但不是贯穿全产品的固定 9:16 手机容器；离开训练阶段后恢复明亮的训练手账视觉。
_Avoid_: 全站视频舞台、固定竖屏手机壳、信息流播放器、装饰性控制栏

**方案摘要列表**:
方案页按动作顺序呈现的紧凑列表，每项默认只显示动作名称、组数、次数或时长和休息。完整参数编辑、来源片段、来源证据和动作要点在用户点击动作后通过 Bottom Sheet 按需展开；排序、复制、删除和手工新增不与开始训练争夺主视觉。
_Avoid_: 动作全字段平铺、桌面表格缩进手机、卡片信息墙、多主按钮

**方案教练卡**:
基础方案顶部由 TrainPal 提供的非阻塞个性化入口，首次显示“让 TrainPal 调整这次训练”，完成后显示“已为本次训练调整 · 查看变化”。它不自动打开 GYMTI、不遮挡覆盖警告，也不比底部固定的“开始训练”更像主操作。
_Avoid_: 强制个性化弹窗、列表内重复解释、主按钮竞争、自动应用调整

**训练主操作**:
训练页底部随确定性训练状态变化的唯一主要按钮，例如“开始本组”“完成本组”或“准备好了”。暂停、跳过、临时调整和提前结束属于次级操作并进入“更多”或 Bottom Sheet，不与当前步骤并列成按钮墙。
_Avoid_: 多个同级训练按钮、Pet 控制状态、自动开始下一组、隐藏提前结束

**训练中小猫位置**:
训练阶段的小猫教练位于动作与控制区域旁，不覆盖来源视频、计时或训练主操作；默认最多同时呈现一条当前要点，点击后才展开完整要点。休息阶段可以放大小猫并与倒计时成为视觉中心，训练阶段则让位于动作内容。
_Avoid_: 视频浮层 Pet、同时展示三条要点、装饰遮挡、训练阶段抢占主体

**训练意图**:
用户已经产生规划或完成一次训练的目标。训练意图可以在用户尚不具备立即训练的时间、地点或器械条件时产生。

**当次视频意图**:
用户选择某条视频进行训练编译所表达的当次训练意图，在与长期 GYMTI 目标冲突时优先。TrainPal 不因 GYMTI 删除、替换或否定视频动作；无法在来源边界内合理调整时，可以正常返回不修改的方案。
_Avoid_: GYMTI 纠正选片、强制目标匹配、为了体现 AI 而修改

**训练条件**:
用户开始一次训练所需的时间、地点、器械和身体准备状态。
_Avoid_: 训练场景

**主动编排**:
用户已有训练意图，并主动寻找和组织适合自己的训练动作，为一次后续训练做准备。
_Avoid_: 被动触发

**延后收集**:
用户在浏览时遇到以后想练的训练动作，但当下不具备训练条件，因此先保存，之后再用于主动编排。
_Avoid_: 立即训练

**待练清单**:
延后收集的训练动作集合。用户可以从中多选动作进入主动编排，但待练清单本身不是训练方案或原视频收藏夹。
_Avoid_: 视频收藏、训练方案

**我的训练**:
用户管理未完成训练、方案草稿、待练动作、训练方案、训练记录和训练档案的统一入口。
_Avoid_: 周目标中心、健身社区

**训练动作**:
用户可以独立理解、选择并纳入一次训练的动作单元。训练动作可以来自视频，也可以由用户在没有参考视频时自行创建。
_Avoid_: 完整视频、训练视频

**次数型动作**:
以完成目标次数作为一组结束条件的训练动作。用户需要主动确认本组完成。
_Avoid_: 自动计数动作

**时长型动作**:
以完成目标持续时间作为一组结束条件的训练动作。用户确认开始后由计时器判断本组完成。
_Avoid_: 次数型动作

**视频动作**:
从来源视频中识别或选取的训练动作，保留原视频和来源片段以供训练时观看和找回出处。
_Avoid_: 自建动作、完整视频

**自建动作**:
用户在没有参考视频时自行创建的训练动作。自建动作可以进入训练方案，但不提供原视频、来源片段或自动匹配的替代视频。
_Avoid_: 视频动作、待确认动作

**来源视频**:
视频动作被识别或选取时所依据的本地来源媒体或受控视频。它是动作的出处，不是训练编排的基本单元。
_Avoid_: 收藏项、训练单元

**来源片段**:
来源视频中用于观看某个训练动作并找回出处的连续时间区间。它默认只是参考，不自动把片段长度解释为训练时长，也不等同于一份已裁剪生成的新视频文件。
_Avoid_: 演示片段、裁剪视频、默认训练时长

**参考播放**:
没有可靠来源节奏时，训练页跳到来源片段起点并默认播放一次，结束后暂停且允许用户再次播放。动作次数或计时独立运行，不循环片段或使用片段长度充当训练时长。
_Avoid_: 自动循环讲解、片段时长计时、手动拖动找动作

**来源训练时间线**:
动作分析 Agent 从来源视频中识别出的动作、休息、切换和非训练内容顺序，以及各项对应的绝对时间段。所有动作都可以成为候选，只有可靠来源节奏可以成为训练默认值；用户编辑后以当前方案为准，同时保留原视频位置。
_Avoid_: 不可编辑跟练块、永久播放约束、视频剪辑工程

**展开式执行时间线**:
循环训练在当前方案中的表达方式：按照真实执行顺序展开每一次动作出现，例如 `A1 → B1 → A2 → B2`。轮次可以作为展示标签，但不形成嵌套方案；每次出现都是独立动作安排，组数只表示该安排内同一动作的连续组数。
_Avoid_: 嵌套循环组、用组数代替轮次、去重后的动作目录

**来源节奏**:
来源视频明确且可靠表达的一段连续动作、休息和切换顺序，以时间线级节奏块保留并可以作为 `video` 来源的训练默认值。编译时把节奏块展开为扁平执行步骤；无法可靠判断时只保留来源片段，不要求用户判断片段属于教学还是跟练。
_Avoid_: 教学/跟练标签、片段长度推断时长、强制片段用途确认

**重复动作编译**:
可靠来源节奏内重复出现的动作保留为不同执行步骤；没有可靠来源节奏时，标准动作、器械和变式相同的重复演示合并为一个方案动作，并选择证据最完整的连续片段作为训练页参考，其他范围只保留在来源证据快照中。器械、变式或视频明确参数不同的同名动作保持分开；时间重叠但名称冲突的候选合并为一个待确认动作。
_Avoid_: 来源节奏去重、重复教学动作堆叠、多参考片段播放器、冲突候选重复展示

**片段修正**:
用户调整来源片段的开始时间、结束时间或动作名称，以修正系统识别结果。片段修正不会生成新的媒体文件。
_Avoid_: 视频剪辑、视频导出

**动作候选**:
内容理解阶段根据来源视频和可用证据识别出的一个或多个可能训练动作，是训练编译 Skill 的内部输入。候选可以缺少参数或可靠时间边界，但产品不再要求用户先处理独立候选列表再进入方案。
_Avoid_: 基础方案提案、已确认动作、用户候选收件箱

**基础方案提案**:
训练编译 Skill 根据完整内容理解结果按来源顺序生成的可编辑、可执行方案建议，包含可靠动作、来源片段、展开后的来源节奏、规则补全值、字段来源、来源证据快照和必要的不确定标记。待确认动作原位保留但默认排除，不阻塞可靠动作；提案在用户确认前不写入或覆盖方案草稿。
_Avoid_: 动作候选列表、方案草稿、已保存方案、自动覆盖

**待确认动作**:
基础方案提案中因动作名称、来源范围或训练方式存在关键歧义而不能直接执行的动作。它保留在来源顺序中但默认排除，用户可以确认、修改或删除；多个歧义不会触发连续问答或阻塞可靠动作。
_Avoid_: 分析失败、必选动作、阻塞整份提案、开放式追问

**动作识别**:
用户明确点击“分析视频动作”后，系统当场分析整段来源视频并返回动作候选与来源训练时间线的过程。页面加载、播放、暂停或拖动进度条都不会自动触发；失败时用户可以重试或手动创建动作。
_Avoid_: 后台识别队列、自动保存

**内容理解 Provider**:
把来源视频及可用证据转换为完整结构化来源训练时间线的可替换能力边界，可以由 TrainPal 自建分析链路或未来的平台内容理解能力实现。它不决定个性化训练参数、不写入方案，也不是面向用户的 TrainPal Agent 或领域 Skill。
_Avoid_: 训练编译 Skill、个性化调整、方案生成器、用户可见 Agent

**内容理解结果**:
内容理解 Provider 交给训练编译 Skill 的完整结构化结果，包含来源标识、视频时长与覆盖状态、按来源顺序排列的动作、来源片段、视频明确参数、来源证据快照、不确定标记和可选来源节奏。它不包含原始转录、模型响应、GYMTI、个人信息或训练历史。
_Avoid_: 原始媒体包、用户画像、基础方案提案、模型日志

**内容理解覆盖状态**:
内容理解 Provider 对本次来源视频实际检查范围的声明，只能是 `complete | partial | insufficient`。`complete` 表示覆盖完整视频但不保证每个语义判断绝对正确；`partial` 必须列出已覆盖与未覆盖范围，可靠动作仍可编译；`insufficient` 表示没有足够可靠证据形成基础方案，用户可以重试或手工创建动作。静音或没有字幕不能单独导致 `insufficient`，仍必须尝试视觉理解。
_Avoid_: 模型准确率、完整即全对、局部结果伪装完整、静音即失败

**覆盖缺口**:
`partial` 内容理解结果中未被可靠检查的绝对时间范围，显示在基础方案动作列表之外并提供重试入口。覆盖缺口不是动作，也不阻塞已识别可靠动作进入基础方案；正常完成分析但没有动作证据的范围不属于覆盖缺口。
_Avoid_: 待确认动作、空动作行、无动作证据、隐藏未覆盖范围

**覆盖缺口重试**:
用户针对覆盖缺口重新分析的显式操作。基础方案尚未确认且没有被用户修改时，成功结果可以按来源时间生成更新版本的整份提案；方案已经确认或编辑后，任何重试或迟到结果都不能自动插入，重新分析必须形成新提案，由用户决定追加或替换。`insufficient` 不生成空基础方案，只提供重试和手工创建动作。
_Avoid_: 静默合并、迟到动作插入、空方案、后台自动重试

**动作分析 Agent**:
首版视频理解阶段的协调能力，自主决定如何把用户选定的视频转化为可练清单，并协调多个分析 Skill 判断是否存在可靠来源节奏。没有可靠节奏时只保留来源片段，不阻塞候选进入方案；它不替用户确认或保存方案，也不控制训练执行。
_Avoid_: 开放式 AI 私教、一键处方生成器、训练执行器

**任务型分析**:
用户在视频场景中明确触发、由动作分析 Agent 完成并返回候选列表的一次分析过程。它不需要聊天对话，可以在用户暂时离开入口时继续，但不成为跨设备或长期后台任务。
_Avoid_: AI 聊天、开放式问答、永久后台分析队列

**分析请求**:
一次任务型分析的独立运行实例，只对用户明确选择的来源视频有效。页面切换、刷新或短暂断线后可以恢复；用户主动取消或更换来源后请求失效，迟到结果不得回填，重试产生新的请求。
_Avoid_: 跨设备任务、跨视频结果、失败请求续跑

**受控视频源**:
由团队预先配置、确认使用权并通过稳定来源标识提供的视频。在下一轮原型中，它用于快速体验和本地视频导入不可用时的兜底，不代表用户自己找到的内容。
_Avoid_: 用户本地视频、任意 URL 代理、信息流爬取

**分析阶段**:
动作分析请求当前正在执行的真实管线阶段，例如准备媒体、语音识别、视觉定位或候选融合。界面可以展示阶段名称，但不把未知完成度伪装成百分比进度。
_Avoid_: 虚假进度、模型思维过程

**分析过程预览**:
整段分析尚未完成时展示的真实处理位置和暂时发现，例如已处理到的来源时间与可能动作数量。过程预览只用于理解进展；动作在整段融合完成前不可编辑或加入方案。
_Avoid_: 最终动作候选、虚假百分比、可编辑中间结果

**分析覆盖缺口**:
来源时间线中因系统或提供方错误而无法判断内容的明确时间段。它不是“已分析但没有动作证据”，必须向用户标明范围并允许单独重试或手工补充。
_Avoid_: 空结果、无动作片段、静默跳过

**部分分析结果**:
一次分析结束时同时包含可靠动作候选和一个或多个分析覆盖缺口的结果。已完成部分可以校正并加入方案，但产品不能把它表述为完整分析；每个缺口都必须保留可重试入口。
_Avoid_: 完整分析结果、整次失败、无提示降级

**可恢复分析任务**:
用户明确触发整段分析后形成的同设备短时任务。页面切换、刷新或短暂连接中断不取消任务，用户返回后可凭运行标识恢复真实状态与结果；只有用户主动取消、更换来源，或达到部署配置的安全边界时才终止。具体等待时间和安全超时不是写死的产品承诺，必须通过实测后配置，界面不得展示未经测量的倒计时或预计完成时间。
_Avoid_: SSE 断开即取消、固定等待承诺、跨设备任务、永久后台队列

**整段分析范围**:
用户明确触发后覆盖完整来源视频的分析边界。当前竞赛版只支持不超过 5 分钟的视频，能力接口必须返回真实的 300 秒上限；竞赛演示仍优先使用较短视频。超过上限时明确提示先在应用外裁剪，不能用内部时间范围或少量稀疏帧冒充完整分析；时间范围只用于用户针对已声明覆盖缺口发起重试。
_Avoid_: 局部时间点猜测、自动触发分析、多分钟稀疏抽样、不限时承诺

**分析 Skill**:
供动作分析 Agent 调用的单一、可复用分析能力，接收明确输入并返回结构化结果。分析 Skill 不拥有用户方案或训练状态，也不自行决定后续操作。
_Avoid_: 自主 Agent、页面功能、训练状态机

**训练语音理解 Skill**:
从带时间信息的字幕或音频中提取动作名称，以及视频明确表达的组数、次数、时长和休息要求。它不推测创作者身体数据或用户负重。
_Avoid_: 训练处方生成、创作者体型估算

**动作视觉定位 Skill**:
根据整段来源视频的均匀时间线画面识别动作变化与演示范围，并返回可能的开始和结束时间及视觉依据。它不判断人物身体数据、伤病风险或训练效果。
_Avoid_: 身体测量、动作质量评分

**动作候选融合 Skill**:
合并语音理解与视觉定位结果，形成去重且带来源依据的动作候选。它保留不确定项，不为缺失的信息编造确定值。
_Avoid_: 自动确认动作、一键生成方案

**候选依据**:
支持某个动作候选的带时间信息的语音或视觉信号，用于内部测试、复核和问题定位。用户界面不展示数值置信度或完整分析日志，只在候选不完整时提示“需确认”。
_Avoid_: 模型思维过程、用户可见调试日志

**分析临时材料**:
为一次任务型分析产生的音频、字幕和关键帧，只服务于当次分析，不成为用户训练数据或长期评测数据。分析结束后只保留来源引用、动作结果、依据类型和非内容诊断信息。
_Avoid_: 用户媒体副本、自动评测语料、长期媒体缓存

**动作识别评测集**:
由团队明确整理、版本化并人工标注的视频样本集合，用于分别验证各分析 Skill 和完整动作分析链路。它同时包含可识别、部分可识别和应当拒绝生成候选的场景，不包含自动收集的真实用户内容。
_Avoid_: 演示视频单例、线上用户语料、主观目测验收

**训练方案**:
为了完成一次训练而组织的一组有明确执行顺序的动作安排。
_Avoid_: 视频合集、长期训练周期

**方案草稿**:
用户正在编排但尚未主动保存为可复用方案的唯一当前训练方案，自动保存以便退出后继续编辑。确认新提案时，空草稿直接写入；非空草稿由用户明确选择追加或确认替换，不同时管理多个未命名草稿。
_Avoid_: 方案库、未完成训练、多草稿工作区、静默替换

**快速体验方案**:
团队为竞赛 Web 版预先配置并明确标注的训练方案样例，供访客无需等待动作分析即可体验训练闭环。它只在用户选择后复制进方案草稿，只有用户主动另存为才进入方案库；不得表示为动作分析 Agent 的识别结果、真实 AI 的降级结果或用户训练记录。
_Avoid_: AI 方案、动作候选、运行时假回退、自动保存方案

**方案库**:
用户主动命名并保存的一组可复用训练方案。编辑方案时直接修改原方案；用户需要保留原方案时，可以另存为新方案。开始一次训练不会自动把方案草稿加入方案库。
_Avoid_: 训练历史、方案草稿

**动作安排**:
某个训练动作在训练方案中的一次具体配置，包括顺序、组数、次数或时长、重量以及休息时间。同一个训练动作可以形成不同的动作安排。
_Avoid_: 训练动作、视频片段

**方案结构校验**:
开始训练前对方案是否具备可执行数据进行的检查。它不评价动作顺序、训练量、伤病风险或长期训练合理性。
_Avoid_: 科学训练评估、伤病评估

**训练安全提示**:
开始训练前提供的非阻塞提醒，要求用户根据自身情况调整并在身体不适时停止。它不是健康问卷、伤病评估或专业训练处方。
_Avoid_: 健康评估、风险诊断、强制确认弹窗

**休息建议**:
系统为动作安排提供的默认休息时间。它优先采用来源视频中明确表达的休息要求；视频没有相关要求时，才使用训练规则估计，且不能覆盖用户已经手动修改的休息时间。
_Avoid_: 固定休息时间

**动作参数建议**:
系统从视频中明确表达的训练要求提取出的组数、次数、持续时间和休息时间默认值。视频中创作者使用的重量不属于动作参数建议，不能直接成为用户负重。
_Avoid_: 用户处方、创作者负重

**训练编译**:
系统在保留来源动作、原视频时间点和创作者明确要求的前提下，把内容理解结果转化为一场可编辑、可立即执行的训练。它可以补足缺失参数或把范围具体化，但不以生成一套脱离原视频的新处方为目标。
_Avoid_: 视频摘要、一键重写方案、AI 训练处方

**训练编译 Skill**:
只接收内容理解 Provider 输出的完整结构化来源时间线，并生成基础方案提案的可复用能力。它不读取原始视频、音频、字幕或个性化上下文，不调用视觉或语音模型，也不写入用户草稿；缺失参数由版本化确定性规则补齐。
_Avoid_: 原始媒体分析、内容理解 Provider、个性化调整、训练执行器、候选列表页面

**个性化调整 Skill**:
只在用户主动触发后，接收基础方案和个性化上下文，在确定性边界内选择数值、解释理由并返回整套个性化调整提案的可复用能力。它不直接应用改动、不控制训练执行、不替换或重排动作，也不把单次方案数值写成全局默认值。
_Avoid_: 自动个性化、训练编译 Skill、全局训练处方

**动作要点补充 Skill**:
只接收已确认身份动作的动作 ID、标准化名称、来源证据快照、视频明确要点、可用的器械或变式信息，以及安全与来源优先级规则版本，非阻塞地生成带依据标签的动作要点集合。它可以调用联网搜索工具或模型世界知识，但不读取待确认动作、原始媒体、GYMTI、个人信息、训练参数或训练历史，不修改方案或控制训练执行。
_Avoid_: 待确认动作补充、实时动作纠正、训练中联网、个性化调整 Skill、网页搜索工具本身、用户画像

**动作要点补充结果**:
动作要点补充 Skill 针对每个已确认动作独立返回的结果，状态只能是 `ready | no_reliable_tip | failed_retryable`。`ready` 最多包含三条正文、`video | web | model` 依据、视频证据引用或网页标题与链接，以及安全过滤结果或警告；没有安全且可靠的内容时可以合法返回空结果。
_Avoid_: 整份方案统一成败、无限要点、无依据要点、内部模型日志

**动作要点局部重试**:
一个动作补充失败或没有可靠要点，不影响其他动作、基础方案、编辑或开始训练。成功动作的结果保持不变；用户或系统重试只处理 `failed_retryable` 或 `no_reliable_tip` 动作，不静默重新生成已经成功的要点。
_Avoid_: 全方案重跑、成功结果漂移、失败阻塞训练、静默重新生成

**TrainPal 任务信封**:
TrainPal Agent 调用领域 Skill 时共用的最小任务合同，只统一 `task_id`、任务类型、合同与 Skill 版本、输入版本和输入指纹；训练编译、个性化调整和动作要点补充各自使用独立的小型输入输出载荷。相同幂等键的重复请求复用同一次运行或成功结果，它不是包含所有用户、媒体和训练状态的万能上下文。
_Avoid_: 万能 Agent 状态、原始媒体载荷、共享大 Schema、跨 Skill 隐式字段

**TrainPal 编排**:
TrainPal Agent 使用确定性外层流程依次完成内容理解、训练编译和基础方案可用；基础方案形成后，动作要点补充按动作异步运行，个性化调整只在用户主动触发时运行且不等待动作要点。待确认动作被用户确认后只启动该动作的要点补充；三个 Skill 不互相调用，也不进入可无限规划、反思或工具调用的开放循环。
_Avoid_: Skill 互调、开放式 Agent 循环、要点阻塞方案、自动个性化

**TrainPal 任务时效**:
每次 Skill 任务绑定 `task_id + input_fingerprint + contract_version + skill_version`。相同幂等键的重复提交不重复调用模型；成功结果持久化并在刷新或返回页面时复用。只有用户明确重试或重新生成才创建新任务；输入变化、任务被替换或页面已经接受更新版本后返回的迟到结果必须丢弃，不能覆盖当前方案。
_Avoid_: 刷新重跑、重复模型调用、迟到结果覆盖、新旧输入混用、成功结果静默漂移

**个性化允许范围**:
确定性工具根据视频明确范围和版本化规则为 TrainPal 建议建立的候选边界。个性化调整 Skill 只能在边界内选择并解释；修改视频明确值必须突出理由，动作结构和重量始终不由 Agent 调整，但该边界不限制用户手动编辑。
_Avoid_: LLM 自由数值、Agent 隐式越界、Agent 动作替换、Agent 自动重量、限制用户编辑

**个性化调整失败**:
个性化 Skill 无法生成通过校验的完整提案时，基础方案保持可用并允许重试、手动编辑或直接训练。系统不展示部分修改，也不把规则值伪装成个性化结果。
_Avoid_: 基础方案失败、部分个性化、假降级、丢失用户资料

**个性化补全**:
用户主动进入个性化流程后，TrainPal 结合个性策略、训练经验和历史实际完成补足缺失参数或具体化范围。改动视频已经明确的要求必须说明理由并由用户确认；系统不猜测器械可用性或重量，用户通过删减动作和最终调整建议值保证方案适合自己。
_Avoid_: 默认强制个性化、器械问卷、身体数据决定训练量、自动改写来源要求

**让 TrainPal 调整这次训练**:
用户在基础方案形成后主动进入的个性化流程，首次使用时完成包含训练经验信号的 GYMTI；题目完成后先经过可跳过的“完善训练档案”，再在结果页查看由独立风格通道形成的唯一小猫教练推荐。用户必须明确确认推荐，或通过“修改风格”入口选择并确认其他正式风格后才保存风格，随后查看本次训练的调整提案。它不阻塞用户直接按基础方案开始训练，也不自动覆盖已经确认的方案。
_Avoid_: 按我的情况补全、强制入门问卷、一键生成方案、自动应用调整

**训练经验**:
用户对自己独立理解并完成常见训练动作与训练安排能力的自我描述，由 GYMTI 问卷采集，不设置独立填写入口。它不从训练时长或身体数据自动推断，具体分级随问卷合同另行确定。
_Avoid_: 独立训练经验表单、器械可用性问卷、身体数据推断、纯健身年限分级

**个性化调整提案**:
TrainPal 针对生成时的基础方案和个性化上下文给出的整套字段差异集合，在用户整体确认前不改变方案；没有差异也是合法成功结果。成功提案保持原样直到用户明确重新调整；用户值不进入调整范围，确认后的建议值保留 `personalized` 来源，随后手动修改的字段改为 `user`。
_Avoid_: 重写后的完整方案、自动应用、逐字段审批、无理由改写、静默重新生成

**个性化差异项**:
个性化调整提案中针对一个可调整字段的修改，必须声明动作与字段、修改前值、修改后值、简短理由和 `personalized` 来源。它不能改变动作结构或重量，也不能覆盖用户值。
_Avoid_: 整份方案替换、隐式字段修改、无理由建议、用户值覆盖

**个性化字段优先级**:
用户输入或手动修改的值高于全部来源规则，且不受 Agent 个性化允许范围约束；旧个性化值在用户再次触发时可以重算，视频明确值默认保留且修改时必须突出理由，视频范围和规则值可以由 Agent 在允许范围内调整。接受新提案只更新明确展示且尚未被用户修改的字段。
_Avoid_: Agent 覆盖用户值、用建议边界限制用户、静默修改视频值、整份方案重写

**用户调整权**:
用户可以修改动作名称、顺序、训练方式、组数、次数或时长、休息、重量和参考时间段，也可以新增、复制或删除动作，并以自己的选择覆盖视频、规则或个性化建议。修改后的字段标记为 `user`；系统只保留使方案能够执行的结构校验，不把 TrainPal 的推荐范围当作用户输入上限。
_Avoid_: Agent 最终决定权、建议值强制锁定、个性化范围限制用户、锁定视频字段

**来源证据快照**:
内容理解完成时形成的只读结构化出处，保留原始动作名称、来源时间段、视频明确参数和证据类别，用于区分视频原意与后续用户修改。用户编辑不会改写快照；快照不包含原始转录、提示词、模型响应或内部日志。
_Avoid_: 当前方案值、可编辑视频原意、模型原始响应、永久转录

**方案可执行性校验**:
只阻止无法形成训练步骤的结构和值，例如空动作名、无效训练方式、缺失或非正数的组数与次数或时长、负休息时间，以及越出来源视频的参考时间段。超出 TrainPal 建议范围但仍可执行的用户值最多显示非阻塞提示，不阻止保存或开始训练。
_Avoid_: 个性化推荐范围、训练处方审核、异常值强制拦截、静默改值

**提案时效**:
个性化调整提案与生成时的基础方案版本和个性化上下文快照关联。基础方案变化会使旧提案不可应用；GYMTI、教练风格或个人信息变化只会把旧提案标记为基于旧了解，用户仍可应用或明确重新调整。
_Avoid_: 自动套用失效方案、上下文变化强制重算、静默刷新成功提案

**恢复基础方案**:
用户撤销当前方案全部 `personalized` 调整并恢复个性化前基础值的行为。它不删除 GYMTI、教练风格、训练档案、训练经验或历史训练信号。
_Avoid_: 清除个性化上下文、撤销用户手动编辑、删除方案

**个性化上下文**:
TrainPal 跨视频持续使用的全局用户上下文，由稳定个性信息和带场景的训练信号组成。它用于动态生成新的个性化提案，但不是一组跨视频套用的数值默认值，也不包含模型自由编写的用户画像。
_Avoid_: 全局训练参数、黑盒用户画像、自动改写历史方案、单视频调整副本

**稳定个性信息**:
只能由用户明确设置或修改的 GYMTI、训练经验、小猫教练风格和可选训练档案。TrainPal 不根据行为或模型推断静默改写这些信息。
_Avoid_: 自动人格漂移、隐式 GYMTI、模型生成档案

**训练信号**:
与具体动作、方案或训练场次关联的结构化事实，包括用户轻反馈、实际完成、场次调整接受或拒绝、个性化提案接受或拒绝和参数再次修改。单条信号不直接成为长期偏好或全局数值默认值，只在用户以后主动生成个性化提案时作为带场景的参考。
_Avoid_: 全局参数、脱离场景的偏好结论、自由文本画像、后台自动个性化

**动作规则默认值**:
视频没有明确训练参数时，由版本化、可测试的确定性产品规则提供的可修改初始值。它不由 LLM 临场生成，不得表示为视频建议或个性化建议。
_Avoid_: AI 推测参数、视频原始参数、专业训练处方、隐式规则版本

**字段来源**:
动作安排中某项训练参数的出处，只能是视频明确表达、产品规则默认值、TrainPal 个性化建议或用户修改，对应 `video | rule | personalized | user`。用户值优先，创作者重量不能成为视频来源值。
_Avoid_: 数值置信度、未标注默认值、模型猜测


**卡路里估算**:
系统综合训练档案、训练方案或实际训练过程计算的单一近似能耗值。训练档案不完整时仅提供弱化的通用约值且不阻止训练，档案完整后才提供个性化约值。
_Avoid_: 精确卡路里、卡路里区间、默认体重个性化

**训练档案**:
用户可选填写的年龄、性别、身高和体重，用于卡路里约值，也可以在既有视频或规则边界内作为个性策略的弱参考。它不能独立生成参数、突破边界、覆盖视频明确值、推断重量、健身人格或健康状态；缺失时不阻止使用，首版不包含体脂率。
_Avoid_: 必填身体信息、身体人格、体脂档案、独立强度处方

**完善训练档案**:
GYMTI 题目完成后、正式结果展示前，在同一页面鼓励用户一次性补充年龄、性别、身高和体重的可跳过步骤。四项放在同一张卡片，390px 使用两列、320px 改为单列；页面明确提示“可跳过，不影响测评结果”，底部提供“保存并查看结果”和“跳过”。明确提交的档案独立保存，不进入测评或推荐，也不因用户放弃确认教练风格而回滚；只有已有档案时显示“清除已有档案”，跳过表示保持不变。
_Avoid_: 个性化负荷信息、身体测评、个性化处方、必填档案、多步身体问卷、跳过即清除、个性化约值已开启

**个人信息弱参考**:
训练档案只有在视频证据、规则边界和主要训练上下文已经形成候选范围后，才能辅助选取组数、次数、时长或休息。它不能单独生效，最终结果标记为个性化建议并由用户确认或调整。
_Avoid_: 身体数据默认值、人口统计处方、个人信息决定训练量

**GYMTI 健身人格**:
用户通过测评表达的健身目标取向，用于决定 TrainPal 的目标优先级、成果表达和个性化建议方向。它可以具体化视频参数范围、参与缺失参数补全或提出需确认的修改建议，但不单独改写视频动作结构，也不与某种教练风格强制绑定。
_Avoid_: MBTI、健康诊断、身体人格、训练强度等级

**GYMTI 问卷尝试**:
用户从进入测评到正式生成结果或主动退出的一次过程。第 5 至第 8 题优先消解当前并列倾向；第 8 题一旦作答必须生成包含唯一主要类型与唯一教练风格推荐的正式结果。用户可以在结果生成前退出，退出不产生推荐，也不改变任何已确认风格。
_Avoid_: 无限追问、第八题无结果、退出即保存

**GYMTI 提前完成条件**:
第 5 至第 7 题作答后，用版本化评分合同判断当前信号是否已经足以提前形成唯一主要类型和唯一教练风格推荐的门槛。它只决定问卷何时结束，不决定用户最终是否能获得结果；未满足时继续自适应选题，第 8 题改用终局题保证结果。
_Avoid_: 用户可见置信度、提前完成失败即无结果、前端自定义阈值

**GYMTI 自适应选题**:
LLM 根据当前问卷状态，从版本化题库的合法候选中选择下一道题；相同的局部答题状态可以进入不同的后续题目路径，这种不可复现性是问卷探索感和神秘感的一部分。LLM 不能现场生成或修改题干、选项与计分映射；模型超时、报错、返回非法题目或当前会话没有模型预算时，系统在有限重试后由本地版本化规则从规范排序后的合法候选中确定性选题，不中断问卷也不冒充模型带来的路径差异。选题变化只能影响接下来收集哪些信号，不能改写已经提交的回答、绕过总题数与结果成立条件，或直接保存教练风格。
_Avoid_: 强制固定题序、要求相同路径复现、LLM 现场出题、模型失败阻塞问卷、LLM 改写历史回答、选题即结果

**GYMTI 选题上下文**:
主应用只把问卷与规则版本、已答题目和选项的稳定 ID，以及合法候选题目的稳定 ID 发送给无状态 API；服务端用共享合同重新校验候选，并从已验证回答派生供模型使用的语义标签。模型输入不包含展示用题干或选项文案、训练档案、用户身份信息、训练历史、已确认教练风格或其他自由用户画像。
_Avoid_: 完整答题文案、身体信息、行为画像、无关用户上下文、原始客户端状态

**GYMTI 模型增强**:
竞赛环境通过 Ark／豆包对合法候选选题和既定结果叙事提供可选变化感。它只接收版本化稳定 ID、服务端派生的最小结构化信号和已经成立的结果字段，使用独立于真实视频分析的非排队三并发门；第四条并发、模型错误或留存边界未确认时立即使用本地版本化降级，不能阻塞问卷或改变正式计分。
_Avoid_: DeepSeek 原型默认、展示文案外传、身体档案、评委码门禁、占用视频分析槽、排队等待模型

**GYMTI 终局题**:
第 7 题后仍未达到提前完成条件时使用的第 8 题强制选择题。服务端只把固定版本题库中“每一个可见选项都能使当前状态形成唯一主要类型和唯一教练风格推荐”的题目列为合法候选，再由 LLM 选择具体题目；终局题不提供“都不像”。用户可以在作答前退出且不产生结果，一旦提交任一选项就必须形成正式结果。
_Avoid_: 普通随机题、都不像、数组顺序破平局、默认人格、默认猫、作答后无结果

**待展示 GYMTI 结果**:
答题已经完成、但结果页尚未首次成功展示的新测评结果。首版同时至多保存一个待展示结果；它被保存用于继续流程，但不替换当前 GYMTI 结果、当前推荐或已确认的小猫教练风格。
_Avoid_: 后台生效结果、隐式人格更新、临时默认猫

**GYMTI 问卷结果**:
一次完成的 GYMTI 问卷尝试在结果页首次成功展示后形成的当前正式结果。每个结果包含且仅包含一个主要 GYMTI 类型和一个小猫教练风格推荐，可以附带一个次要倾向，但不存在无类型结果、多候选推荐、无依据的默认猫或面向用户的低、中、高置信度标签。
_Avoid_: UNSET 结果、待确认人格、候选列表、无推荐结果、置信度徽章

**当前 GYMTI 结果**:
首版唯一保留的正式 GYMTI 结果，包含本次答题稳定 ID、问卷与规则版本、正式类型、推荐风格、原因码和结果叙事快照。新结果首次成功展示时原子化替换旧当前结果并删除旧答题明细；产品不提供历史结果列表，已确认的小猫教练风格作为独立状态不随替换改变。
_Avoid_: GYMTI 历史档案、多个当前结果、旧结果影响新推荐、重测自动换猫

**GYMTI 本地状态**:
由主应用 Dexie 持久化的一个进行中尝试、一个待展示结果和一个当前结果，是问卷恢复与结果生命周期的权威状态。API 不建立问卷会话，只根据客户端发送的最小稳定 ID 与版本重新校验、计分并调用 LLM，不保存逐题回答、训练档案、结果或请求正文。
_Avoid_: 服务端内存会话、双写权威、第二套问卷数据库、API 保存答题正文

**GYMTI 版本化问卷合同**:
Vue 与 FastAPI 共同读取的单一 JSON 事实源，包含稳定题目与选项 ID、展示文案、语义标签、计分、风格安全排除、探查目标、原因码和版本。Vue 使用它渲染问卷、恢复本地状态和执行降级；FastAPI 使用它无状态复算与校验。原型 Markdown、CommonJS 服务和静态页面不是运行时合同。
_Avoid_: 两套计分配置、运行时解析 Markdown、独立 Node 问卷服务、iframe、前后端规则漂移

**GYMTI 视觉整合**:
正式问卷以现有 Vue TrainPal 设计系统为唯一视觉权威，使用手账网格背景、`tp-*` 语义 Token、统一中英文字体、卡片、胶囊按钮、沉浸式页面和安全区规则。问卷原型只提供题目内容、GYMTI 插画和可复用形象素材，不保留深色遮罩抽屉、独立字号与控件、候选轮播或底部导航。
_Avoid_: 第二套设计系统、全屏遮罩问卷、原型按钮复刻、候选 1/7、换一只哈基米、问卷内底栏

**GYMTI 问卷页头**:
自适应题目页使用左侧返回、中间 `GYMTI`、右侧 `第 n 题` 的统一沉浸式页头，并在题目区提示“通常 5–8 题”；第 8 题改为“最后一题”。由于问卷可能提前结束，不展示百分比或固定八段进度；结果页标题统一为 `GYMTI · 测评结果`。
_Avoid_: 固定完成百分比、八段进度条、RESULT · 8题、独立关闭大按钮

**GYMTI 插画与动效**:
七张 GYMTI 低多边形人物插画保留人物主体与柔和背景，但裁去图片内置标题和小字，由 Vue 使用 TrainPal 字体重新排版。答题页与档案页不展示人格插画或小猫；结果页只展示当前 GYMTI 静态插画和推荐猫待机动画，改选页只有临时选中猫动画。低动效模式使用静态首帧并取消题目切换动画。
_Avoid_: 原图内嵌标题、答题暗示插画、未确认猫陪答、多猫同时动画、低动效轮帧

**GYMTI 答题返回**:
第 2 题以后返回上一道已答题并恢复原选择；修改较早答案时删除其后的回答与题目路径，再由 LLM 基于新状态重新选题。第 1 题返回进入问卷前的页面并保留进行中尝试，之后可以继续；题目页不另设关闭按钮。
_Avoid_: 改答后沿用旧路径、首题返回即清空、返回即生成结果、双退出入口

**GYMTI 选项提交**:
题目页点击任一选项就立即提交并进入下一题，不需要再点击“继续”确认；第 8 题点击选项后直接完成答题并进入“完善训练档案”。提交后短暂保留明确选中态并锁定重复点击，题目页不显示固定底部主按钮；用户需要改答时使用页头返回上一题。
_Avoid_: 二次确认、继续按钮、完成答题按钮、点击后无反馈、重复提交

**GYMTI 选题等待**:
选项提交后保留当前题目和选中态、锁定所有选项，并在题目下方显示“TrainPal 正在挑下一题…”，下一题就绪后直接替换。LLM 转入本地降级选题时保持正常过渡且不展示错误；只有模型与本地规则都无法产生合法题目时，才显示保留尝试的重试／退出故障页。
_Avoid_: 独立加载页、空白闪屏、暴露模型降级、选题失败清空尝试、无限等待

**GYMTI 运行日志**:
用于观察无状态选题与结果叙事调用健康度的非内容型事件，只记录请求 ID、合同版本、模型名、耗时、成功或降级状态、所选下一题 ID 和错误类别。它不记录用户选项、得分、原因码、提示词、模型原文、结果叙事、训练档案或自由用户画像；模型供应商侧可配置的训练与内容留存必须关闭，无法确认时只能使用本地降级。
_Avoid_: 请求正文日志、回答日志、提示词追踪、模型响应归档、供应商训练复用

**GYMTI 结果叙事**:
LLM 在正式结果已经由版本化规则确定后，根据主要类型、可选次要倾向、推荐风格 ID 和本地派生的最多三个正向原因码生成的可变解释文案。原因码取自对最终人格或推荐风格贡献最大的已选选项；“都不像”、扣分项与安全排除只留在内部规则和审计中，不进入用户叙事。LLM 不读取逐题题干、完整回答、训练档案或其他用户画像，不能重新计分、改变结果或补造原因；模型文案不是稳定个性信息。模型超时或失败时，结果页立即使用版本化本地模板，正式结果照常生效，用户仍可确认推荐或修改风格。
_Avoid_: 模型重新判型、原始回答外传、负面人格判断、暴露安全排除、虚构推荐依据、用文案代替结果字段、生成失败阻塞结果

**GYMTI 结果叙事快照**:
每份待展示结果首次生成并随结果保存的解释文案、来源、生成版本与时间。来源可以是 LLM 或版本化本地模板；同一结果再次进入时直接复用快照，不重新调用模型或自动改写，只有重测形成新结果时才生成新快照。
_Avoid_: 每次打开重写、后台替换文案、旧结果调用新提示词、无来源文本

**GYMTI 结果页层级**:
正式结果页先展示 `GYMTI · 测评结果`、GYMTI 插画、主要类型和核心解释，再展示最多三个正向原因与低强调的可选次要倾向；其后使用独立卡片展示推荐小猫教练、共享七猫动画、风格名称和匹配理由。底部唯一主操作为“确认这个风格”，卡片内提供“修改风格”，页面末尾低强调提供“重新测评”。
_Avoid_: 猫咪盖过人格结果、多主按钮、原因长列表、次要倾向并列主结果、重测抢占确认

**GYMTI 次要倾向**:
当主要类型是唯一第一名、第二名具有正向得分且两者分差不超过 1 时，随正式结果展示的可选辅助解释。它不是第二个人格结果，不作为独立稳定身份，也不参与教练风格推荐或确认。
_Avoid_: 零分次要倾向、副人格、双人格、第二结果、风格映射依据

**小猫教练风格**:
由不同品种小猫承载、并由用户确认的 TrainPal 陪伴与沟通风格。首版正式目录为热血鼓励型、温柔陪伴型、毒舌督促型、专业数据型、幽默话痨型、挑战突破型和佛系陪练型。正式的 GYMTI 问卷结果总会给出一个单一推荐，结果页从该推荐开始；用户确认或通过明确的“修改风格”入口改选后才保存。改选入口仅位于个性化结果页与“我的”设置，不在训练中提供。改选页默认以静态形象卡展示目录，只有推荐或当前点选的风格播放待机动画。教练风格只影响语气、鼓励方式、提示密度、反馈表现和结果叙事；不改变动作、顺序、组数、次数、时长、休息、重量或安全边界，任何风格都不得羞辱身体、健康状况或训练结果。
_Avoid_: 小猫皮肤、独立 Agent、训练强度类型

**未确认小猫教练风格**:
用户尚未确认任何正式小猫教练风格的状态。训练与方案仍然可用，TrainPal 的文字提示、计时和训练帮助保持中性语气；正式页面不展示现有的中性 TrainPal 形象或任一具体小猫教练，它不是默认风格。
_Avoid_: 临时默认猫、中性 Pet、阻塞训练

**小猫教练风格推荐**:
针对某一 GYMTI 结果给出的单一小猫教练风格建议。问卷分别累计 GYMTI 人格分和教练风格分；推荐取应用安全排除后的风格通道唯一最高分，不通过人格到猫咪的固定映射生成，因此同一种 GYMTI 可以推荐不同风格。未确认的新推荐只留在 GYMTI 结果流程中，不在“我的”或其他正式页面形成待办、角标或额外入口。
_Avoid_: 人格绑定、固定人格猫咪表、默认风格、自动保存、推荐待办、跨页面催促

**小猫教练风格安全排除**:
当固定选项明确表达某种沟通风格会造成伤害、自我攻击或直接关闭产品时，由版本化问卷规则把对应风格从本次推荐候选中硬排除。安全排除先于风格排名和终局题校验，不能被后续加分覆盖；普通的不喜欢或偏好较弱只使用负分，不触发硬排除。
_Avoid_: 普通负反馈即禁用、后续加分解禁、终局题重新引入、把排除风格保存为推荐

**小猫教练风格改选**:
用户通过明确的“修改风格”入口，在一次展示全部七种风格的正式目录中预览并暂时点选其他风格的过程。390px 使用两列、320px 单列、桌面最多三列；卡片显示静态形象、风格名和一句沟通方式，以小标签区分“本次推荐”和“当前使用”。默认临时选中本次推荐，只有临时选中卡播放待机动画；点击仅更新选中态，底部“确认这个风格”才保存，返回结果页时丢弃临时选择。
_Avoid_: 换一只轮播、筛选目录、全卡动画、浏览即保存、点选即生效、返回保留临时选择

**小猫教练风格确认**:
用户明确接受问卷推荐或当前待确认选择、使其成为稳定个性信息的动作。只有确认能够保存风格；查看推荐、播放预览或进入改选目录均不构成确认。
_Avoid_: 隐式确认、自动写入、预览即确认

**小猫教练视觉资产管线**:
首版七种风格的动画素材、共享目录、播放器和五态适配通过版本化资源清单进入正式 Vue 应用；GYMTI 推荐与改选仍必须使用版本化问卷合同，并在用户明确确认后才写入风格。仅替换或新增视觉素材不能改变计分、推荐、安全排除或确认语义。
_Avoid_: 视觉素材决定推荐、临时推荐规则、假个性化、未确认风格持久化

**小猫动画预览台**:
只在开发或验收环境中访问的动画检查页面，用于逐只查看小猫教练风格及其既有动作。它不进入用户正常导航，也不是用户选择或保存风格的入口。
_Avoid_: 正式设置页、二十二动作产品功能、用户风格选择器

**用户轻反馈**:
用户在训练和使用过程中提供的低成本、非开放问答信号，包括结构化训练反馈和明确的偏好选择。它用于逐步调整个性策略，不等同于姿态判断或健康评估。
_Avoid_: 开放聊天、实时动作识别、心理推断

**个性策略**:
TrainPal 根据 `GYMTI 健身目标 × 小猫教练风格 × 可选个人信息 × 用户轻反馈` 形成的陪伴与建议策略。它影响表达、目标侧重和个性化补全，但实际训练建议仍受视频证据、训练条件和安全边界约束。
_Avoid_: 固定人格脚本、身体数据处方、猫咪决定训练强度

**训练数据**:
用户的训练档案、待练动作、方案草稿、训练方案、未完成训练和训练记录。首版要求同设备持久化并支持用户清除，不要求跨设备同步。
_Avoid_: 抖音账号数据、跨设备档案

**动作要点**:
围绕某个训练动作、随方案保存并在训练中按需呈现的简短提示，每条都必须声明动作要点依据。它分为有可核验出处的来源化动作要点和明确没有外部出处的 TrainPal 通用建议。
_Avoid_: 实时动作纠正、无依据提示、统一 AI 提示

**来源化动作要点**:
围绕某个训练动作，从来源视频明确表达或带有效网页引用的联网结果中整理出的简短训练提示，在方案编译阶段一次生成并随方案保存，每个动作最多保留三条。只有依据为 `video` 或 `web` 的提示使用该名称；训练中不再等待模型或联网，也不根据用户实时姿态判断动作是否正确。
_Avoid_: 模型世界知识、训练中实时生成、动作纠正、姿态诊断、无引用网页建议

**动作要点依据**:
动作要点所依赖的信息类别，只能是来源视频、带有效引用的联网结果或模型世界知识，对应 `video | web | model`。`web` 必须保留可点击引用，`model` 必须明确说明没有外部出处，三者不得互相伪装。
_Avoid_: 统一标成 AI 建议、模型知识伪装网页来源、无引用 web

**动作要点安全边界**:
动作要点只能涉及准备姿势、动作路径、呼吸、节奏和常见代偿等基础训练提示，不提供疼痛诊断、伤病康复、禁忌判断、个体负重建议或效果承诺。用户反馈疼痛或眩晕时不再生成训练调整，只提示停止训练并寻求专业帮助。
_Avoid_: 医疗建议、康复处方、自动负重、无伤承诺、带痛训练建议

**动作要点选择优先级**:
通过动作要点安全边界后，互相冲突的提示按 `video > web > model` 选择，较低优先级内容不进入训练提醒。该顺序用于保留用户所选视频的创作者意图，不表示视频天然比网页或模型更专业、更安全。
_Avoid_: 模型纠正视频、网页覆盖创作者要求、把来源优先级当作专业等级

**TrainPal 通用建议**:
TrainPal 根据模型世界知识生成、但没有可核验外部出处的简短动作提示，依据标记为 `model`。它可以在联网不可用或失败时作为明确标注的补充，但不能称为来源化、专业审核结论或个性化安全建议。
_Avoid_: 来源化动作要点、专业认证、网页搜索结果、医疗建议

**动作要点补充**:
基础方案形成后进行的一次非阻塞丰富过程，优先通过联网搜索生成带引用的来源化动作要点，搜索不可用或失败时可以生成明确标注的 TrainPal 通用建议。完成结果随方案保存；基础方案、方案编辑和开始训练都不等待该过程，训练中不重新搜索或生成。
_Avoid_: 基础方案阻塞、训练时联网、静默模型降级、动态改写已保存要点

**动作帮助卡**:
用户针对当前训练动作选择预设帮助主题后得到的一次性动作要点，必须保留“来自视频”“来自网页”或“TrainPal 通用建议”的依据标签。动作帮助卡不接受自由输入、不形成多轮对话，也不自行调整训练方案。
_Avoid_: 开放问答、聊天记录、自主训练调整

**TrainPal 小猫教练**:
在用户确认小猫教练风格后，TrainPal Agent 于训练编排、执行、休息和结果阶段可出现且可隐藏的角色呈现。它可以展示带依据标签的动作要点、收集用户轻反馈、呈现需确认的有界调整建议并召回休息结束的用户，但不拥有训练状态机、不示范动作，也不判断实时姿态。
_Avoid_: 装饰性 Pet、独立第二 Agent、训练执行器、姿态诊断、数字人、静态页面配图

**TrainPal Agent**:
产品面向用户的唯一 Agent 身份，与产品和小猫教练共同使用 TrainPal 名称。它通过确定性外层流程编排训练编译、可选个性化调整和非阻塞动作要点补充三个领域 Skill，语义判断留在各 Skill 内；训练中通过小猫教练呈现已保存要点、用户轻反馈和需确认的有界建议，确定性的计时、进度与记录仍由训练执行器负责。
_Avoid_: 多个用户可见 Agent、开放式 AI 私教、训练执行器

**TrainPal 状态**:
小猫教练当前所呈现的陪伴状态。首版统一使用待机、训练、休息、暂停和完成五种状态。
_Avoid_: 动作示范状态、训练控制状态

**小猫动画适配**:
每种小猫教练风格在 TrainPal 五态中使用低干扰的持续动画，并只在开始或完成一组、休息倒计时和完成场次等已有确定事件上播放一次风格化短演出，随后回到持续动画。它不增加训练状态；专业数据型的扫描与分析、毒舌督促型的嘲讽与不耐烦均不由训练状态自动触发。
_Avoid_: 二十二态模型、状态机扩展、实时诊断动画、自动负面反馈

**TrainPal 可见性**:
用户是否在训练中显示小猫教练的同设备长期偏好。隐藏小猫教练不影响训练，用户可以在训练中随时恢复显示。
_Avoid_: 当前场次临时隐藏、关闭训练功能

**有效训练日**:
用户在本地自然日内留下至少一条包含实际完成动作的训练记录，即形成一个有效训练日；完整完成和已有实际完成量的提前结束都可以计入，同一天只计一次。它不按训练时长、卡路里、重量或总训练量重复累计。
_Avoid_: 登录日、连续打卡、训练量积分、完整结束专属计数

**陪伴成长**:
TrainPal 与用户共同积累有效训练日并形成的长期陪伴进度，不因中断训练或一段时间未使用而倒退。它只影响小猫教练的表达资源和共同训练回顾，不改变 Agent 权限、个性化允许范围、训练参数或安全边界。
_Avoid_: 强度等级、能力升级、连续签到惩罚、训练量竞赛

**成长里程碑**:
陪伴成长达到阶段节点时解锁的表情、动作、纪念卡或阶段性回顾。竞赛首版只要求显示成长进度、训练后反馈和一个里程碑解锁，不依赖尚未定稿的小猫形象资产定义更多等级。
_Avoid_: 数值能力加成、训练处方解锁、必须连续打卡的奖励

**结构化训练反馈**:
用户用“太累、刚好、太轻”描述刚完成动作的主观负荷。TrainPal 每个动作至多在首个正式组后主动询问一次；“刚好”只形成训练信号，“太累”或“太轻”可以触发场次调整建议，但都不是姿态评价、伤病判断或开放式问答。
_Avoid_: 动作质量评分、实时纠错、训练聊天、自动长期偏好

**有界训练调整**:
TrainPal 根据确定性规则和结构化训练反馈生成的即时建议，只影响当前动作在本场训练中的剩余安排，只能涉及休息、次数、时长或组数，并且必须由用户确认。它不调用 LLM、不修改其他动作或已保存方案、不增加重量；接受或拒绝结果写入训练记录并成为以后主动个性化时可用的训练信号。
_Avoid_: 实时模型等待、自动训练处方、跨动作改写、自动修改方案、自动加重量、无确认改动

**休息召回**:
用户在组间休息时离开训练界面后，由 TrainPal 根据同一个休息截止时间提醒其回来继续训练。独立 Web 只保证产品页面内的倒计时与入口；用户切换到抖音或其他应用后，设备提醒仅在平台支持且用户授权时尽力提供，TrainPal 不会出现在外部应用内。
_Avoid_: 推荐页休息、跨应用浮层、后台动作训练、自动开始、保证系统通知

**设备休息提醒**:
用户主动选择“休息结束提醒我”后，TrainPal 可以请求的可选设备通知能力。拒绝、不可用或投递失败时退回页面内休息召回，不阻塞训练，也不反复请求权限；点击提醒只返回“准备继续”。
_Avoid_: 首次进入即索权、强制通知权限、重复索权、通知后自动训练

**训练场次**:
用户实际执行某个训练方案的一次过程，拥有独立的动作进度、训练时间、休息状态、卡路里估算和小猫教练风格快照。场次开始后的风格改选只作用于后续场次，不改变当前场次快照。
_Avoid_: 训练方案、训练历史

**准备继续**:
休息已经结束，但用户尚未确认开始下一组或下一个动作的训练场次状态。该状态不累计动作训练时间，Pet 复用暂停状态的表现。
_Avoid_: 自动开始、Pet 新状态

**跳过剩余组**:
用户结束当前动作尚未完成部分并继续下一个动作的行为。训练场次保留实际完成量，并将当前动作区分为部分完成或已跳过，不把剩余组计为完成。
_Avoid_: 完成当前动作、结束整场训练

**训练记录**:
已完成或提前结束的训练场次所产生的历史结果，保存当次执行的方案快照和实际完成数据。
_Avoid_: 训练方案、方案库、未完成训练

**训练海报**:
根据已完成训练记录生成的对外分享结果卡片，只呈现方案名称、训练时长、约消耗卡路里、完成动作数和 TrainPal，不包含动作与组数明细、训练档案或来源视频。
_Avoid_: 训练详情、AI 总结、训练方案封面

**未完成训练**:
已经开始但尚未完成或明确结束的训练场次。它可以恢复，但不能被当作已完成训练写入历史。
_Avoid_: 后台训练、已完成训练

**提前结束训练**:
用户明确结束但没有执行完全部动作安排的训练场次。它只记录实际完成的动作、组数、训练时间和卡路里估算，并与已完成训练区分。
_Avoid_: 已完成训练、未完成训练
